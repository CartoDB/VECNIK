// object access shortcuts
var
  Int32Array = Int32Array || Array,
  Uint8Array = Uint8Array || Array,
  m = Math,
  exp = m.exp,
  sin = m.sin,
  cos = m.cos,
  tan = m.tan,
  min = m.min,
  max = m.max,
  sqrt = m.sqrt,
  ceil = m.ceil,
  floor = m.floor,
  round = m.round,
  pow = m.pow;

if (!console) {
  console = {};
}
